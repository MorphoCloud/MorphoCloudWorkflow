name: Workshop Request
description: Issue template to request bulk creation of workshop instances.
title: "Workshop: "
labels:
  - request-type:workshop
  - request-creator:user

body:
  - type: markdown
    attributes:
      value: |
        _Please enter a short descriptive title for the workshop in the title bar above this text"_

  - type: dropdown
    attributes:
      label: Cloud Computing Instance Flavor
      description:
        Workshops run on g3.large only (16 CPUs, 60 GB RAM, half an A100 GPU) —
        the only flavor available for workshop instances.
      options:
        - g3.large - GPU instance (16 CPUs, 60 GB RAM and 50% of A100 GPU)
    validations:
      required: true

  - type: input
    attributes:
      label: Duration (days)
      description:
        Number of days the workshop instances should remain active. Maximum 5
        days — requests longer than 5 days are automatically rejected.
      placeholder: "3"
    validations:
      required: true

  - type: input
    attributes:
      label: Number of Instances
      description: Total number of instances needed for the workshop.
      placeholder: "15"
    validations:
      required: true

  - type: input
    attributes:
      label: Workshop start date
      description:
        The day the workshop begins. Format YYYY-MM-DD (e.g. 2026-06-08).
      placeholder: "2026-06-08"
    validations:
      required: true

  - type: input
    attributes:
      label: Workshop start time
      description:
        The time the workshop begins, in the timezone you select below. 24-hour
        HH:MM (e.g. 09:00).
      placeholder: "09:00"
    validations:
      required: true

  - type: dropdown
    attributes:
      label: Timezone
      description:
        Your local timezone, used to schedule instance creation and cleanup.
      options:
        - US/Canada Pacific (America/Los_Angeles)
        - US/Canada Mountain (America/Denver)
        - US Arizona, no DST (America/Phoenix)
        - US/Canada Central (America/Chicago)
        - US/Canada Eastern (America/New_York)
        - Canada Atlantic (America/Halifax)
        - US Alaska (America/Anchorage)
        - US Hawaii (Pacific/Honolulu)
        - Mexico Central (America/Mexico_City)
        - Brazil Brasilia (America/Sao_Paulo)
        - UK/Ireland/Portugal (Europe/London)
        - Western/Central Europe (Europe/Paris)
        - Eastern Europe (Europe/Athens)
        - UTC
    validations:
      required: true

  - type: textarea
    attributes:
      label: Description
      description: >
        Provide details about your workshop. Approval is contingent upon a valid
        use case in the domain of 3D digital morphology.
      value: |
        **Workshop name:**

        **Target audience:**

        **Workshop content / goals:**

        **Software / data to be used:**
    validations:
      required: true
