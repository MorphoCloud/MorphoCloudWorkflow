name: "Extract workshop fields"
description: "Extract workshop request fields"
inputs:
  token:
    description: "GITHUB_TOKEN or repo scoped PAT"
    required: true
  repository:
    description: "The full name of the repository in which the issue exists"
    required: true
  issue_number:
    description: "Issue number identifying the workshop request"
    required: true
outputs:
  description:
    description: "Workshop description"
    value: ${{ steps.extract.outputs.description }}
  duration_days:
    description: "Workshop duration in days"
    value: ${{ steps.extract.outputs.duration_days }}
  number_of_instances:
    description: "Number of instances requested"
    value: ${{ steps.extract.outputs.number_of_instances }}
  start_date:
    description: "Workshop start date as entered (year-first, e.g. 2026-06-08)"
    value: ${{ steps.extract.outputs.start_date }}
  start_time:
    description: "Workshop start time as entered (24-hour, e.g. 09:00)"
    value: ${{ steps.extract.outputs.start_time }}
  timezone:
    description:
      "Selected timezone option text (IANA name in parentheses, or 'UTC')"
    value: ${{ steps.extract.outputs.timezone }}
runs:
  using: "composite"
  steps:
    - name: Fetch the issue
      id: read_issue_body
      shell: bash
      run: |
        echo "body<<EOF" >> $GITHUB_OUTPUT
        gh issue view ${{ inputs.issue_number }} \
          --repo ${{ inputs.repository }} \
          --json body \
          --jq .body >> $GITHUB_OUTPUT
        echo "EOF" >> $GITHUB_OUTPUT
      env:
        GITHUB_TOKEN: ${{ inputs.token }}

    - name: Issue Forms Body Parser
      id: parse
      uses: zentered/issue-forms-body-parser@v2.2.0
      with:
        body: ${{ steps.read_issue_body.outputs.body }}

    - name: Display parsed data
      shell: bash
      run: |
        printf '%s\n' "$DATA" | jq -r .
      env:
        DATA: ${{ steps.parse.outputs.data }}

    - name: Extract fields
      id: extract
      shell: bash
      run: |
        duration_days=$(
          printf '%s\n' "$DATA" |
          jq -r '."duration-days".text'
        )
        echo "duration_days=$duration_days" >> $GITHUB_OUTPUT

        number_of_instances=$(
          printf '%s\n' "$DATA" |
          jq -r '."number-of-instances".text'
        )
        echo "number_of_instances=$number_of_instances" >> $GITHUB_OUTPUT

        start_date=$(
          printf '%s\n' "$DATA" |
          jq -r '."workshop-start-date".text'
        )
        echo "start_date=$start_date" >> $GITHUB_OUTPUT

        start_time=$(
          printf '%s\n' "$DATA" |
          jq -r '."workshop-start-time".text'
        )
        echo "start_time=$start_time" >> $GITHUB_OUTPUT

        timezone=$(
          printf '%s\n' "$DATA" |
          jq -r '."timezone".text'
        )
        echo "timezone=$timezone" >> $GITHUB_OUTPUT
      env:
        DATA: ${{ steps.parse.outputs.data }}
